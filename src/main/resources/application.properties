spring.application.name=projectmanagement

# Database Configuration (Aiven MySQL)
spring.datasource.url=jdbc:mysql://pms-db-service-ruchithacloud-9d59.c.aivencloud.com:14189/ajay?sslMode=REQUIRED&useSSL=true
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_GAUyWTQz-MGNiSAVpPS
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hikari Connection Pool Settings
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server Configuration
server.port=8080

# Logging
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.modelmapper=DEBUG

# OAuth2 Resource Server
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://13.48.18.145

# External User Service
user.service.url=http://13.48.18.145

# Cache Configuration
spring.cache.type=caffeine
spring.cache.cache-names=users,roles,allUsers
spring.cache.caffeine.spec=maximumSize=1000,expireAfterWrite=10m

# Actuator
management.endpoints.web.exposure.include=mappings
management.endpoint.mappings.enabled=true

# Default Project Statuses
project.default-statuses[0].name=To Do
project.default-statuses[0].sortOrder=1
project.default-statuses[1].name=In Progress
project.default-statuses[1].sortOrder=2
project.default-statuses[2].name=Done
project.default-statuses[2].sortOrder=3

# Sprint Configuration
sprint.prompt.window.hours=24

# Scheduler Cron (runs every minute)
sprint.scheduler.cron=0 * * * * *